local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local isActive = false
local animTrack = nil
local followConnection = nil
local flingActive = false
local flyActive = false
local flyConnection = nil
local SpeedActive = false
local resetCooldown = false

local SpectateActive = false
local SpectateConnection = nil
local SpectateGui = nil
local SpectateCamera = nil
local SpectateTarget = nil

local function doReset()
    if resetCooldown then return end
    resetCooldown = true
    task.spawn(function()
        LocalPlayer:LoadCharacter()
        task.wait(3)
        resetCooldown = false
    end)
end

local function watchCharacter(char)
    if not char then return end
    char.DescendantAdded:Connect(function(obj)
        if resetCooldown then return end
        local name = obj.Name:lower()
        if name:find("handcuff") or name:find("cuff") or name:find("arrest") then
            doReset() return
        end
        if obj:IsA("BallSocketConstraint") or obj:IsA("RopeConstraint")
        or obj:IsA("WeldConstraint") or obj:IsA("HingeConstraint") then
            doReset() return
        end
    end)
    local humanoid = char:WaitForChild("Humanoid", 5)
    if humanoid then
        humanoid:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
            if humanoid.WalkSpeed == 0 and not resetCooldown then
                doReset()
            end
        end)
    end
end

watchCharacter(LocalPlayer.Character)
LocalPlayer.CharacterAdded:Connect(watchCharacter)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BangGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer.PlayerGui

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 250, 0, 405)
Frame.Position = UDim2.new(1, -260, 0, 10)
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.BorderSizePixel = 0
Frame.Parent = ScreenGui
Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 22, 0, 22)
ToggleBtn.Position = UDim2.new(1, -34, 0, 10)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleBtn.Text = ""
ToggleBtn.ZIndex = 20
ToggleBtn.Parent = ScreenGui
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(1, 0)

local tDragging = false
local tDragStart = nil
local tStartPos = nil

ToggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        tDragging = true
        tDragStart = input.Position
        tStartPos = ToggleBtn.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                tDragging = false
            end
        end)
    end
end)

ToggleBtn.MouseButton1Click:Connect(function()
    Frame.Visible = not Frame.Visible
end)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 30)
Title.BackgroundTransparency = 1
Title.Text = "t_18"
Title.TextColor3 = Color3.fromRGB(255, 215, 0)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.Parent = Frame

local dragging = false
local dragStart = nil
local startPos = nil

Title.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement
    or input.UserInputType == Enum.UserInputType.Touch then
        if dragging then
            local delta = input.Position - dragStart
            Frame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
        if tDragging then
            local delta = input.Position - tDragStart
            ToggleBtn.Position = UDim2.new(
                tStartPos.X.Scale, tStartPos.X.Offset + delta.X,
                tStartPos.Y.Scale, tStartPos.Y.Offset + delta.Y
            )
        end
    end
end)

local Input = Instance.new("TextBox")
Input.Size = UDim2.new(0.9, 0, 0, 35)
Input.Position = UDim2.new(0.05, 0, 0, 35)
Input.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
Input.TextColor3 = Color3.fromRGB(255, 255, 255)
Input.PlaceholderText = "Player Name..."
Input.Text = ""
Input.Font = Enum.Font.Gotham
Input.TextScaled = true
Input.ClearTextOnFocus = false
Input.Parent = Frame
Instance.new("UICorner", Input).CornerRadius = UDim.new(0, 6)

local function makeBtn(text, color, yPos)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.9, 0, 0, 35)
    btn.Position = UDim2.new(0.05, 0, 0, yPos)
    btn.BackgroundColor3 = color
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Text = text
    btn.Font = Enum.Font.GothamBold
    btn.TextScaled = true
    btn.Parent = Frame
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

local BangBtn  = makeBtn("Bang", Color3.fromRGB(200, 50, 50), 78)
local ResetBtn = makeBtn("Reset", Color3.fromRGB(150, 100, 0), 121)
local FlingBtn = makeBtn("حماية بانق", Color3.fromRGB(50, 100, 200), 164)

local FlyBtn = Instance.new("TextButton")
FlyBtn.Size = UDim2.new(0.55, 0, 0, 35)
FlyBtn.Position = UDim2.new(0.05, 0, 0, 207)
FlyBtn.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
FlyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FlyBtn.Text = "طيران"
FlyBtn.Font = Enum.Font.GothamBold
FlyBtn.TextScaled = true
FlyBtn.Parent = Frame
Instance.new("UICorner", FlyBtn).CornerRadius = UDim.new(0, 6)

local SpeedInput = Instance.new("TextBox")
SpeedInput.Size = UDim2.new(0.32, 0, 0, 35)
SpeedInput.Position = UDim2.new(0.63, 0, 0, 207)
SpeedInput.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedInput.PlaceholderText = "سرعة"
SpeedInput.Text = "50"
SpeedInput.Font = Enum.Font.Gotham
SpeedInput.TextScaled = true
SpeedInput.ClearTextOnFocus = false
SpeedInput.Parent = Frame
Instance.new("UICorner", SpeedInput).CornerRadius = UDim.new(0, 6)

local WalkSpeedBtn = Instance.new("TextButton")
WalkSpeedBtn.Size = UDim2.new(0.55, 0, 0, 35)
WalkSpeedBtn.Position = UDim2.new(0.05, 0, 0, 250)
WalkSpeedBtn.BackgroundColor3 = Color3.fromRGB(180, 80, 0)
WalkSpeedBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
WalkSpeedBtn.Text = "سرعة اللاعب"
WalkSpeedBtn.Font = Enum.Font.GothamBold
WalkSpeedBtn.TextScaled = true
WalkSpeedBtn.Parent = Frame
Instance.new("UICorner", WalkSpeedBtn).CornerRadius = UDim.new(0, 6)

local WalkSpeedInput = Instance.new("TextBox")
WalkSpeedInput.Size = UDim2.new(0.32, 0, 0, 35)
WalkSpeedInput.Position = UDim2.new(0.63, 0, 0, 250)
WalkSpeedInput.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
WalkSpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
WalkSpeedInput.PlaceholderText = "سرعة"
WalkSpeedInput.Text = "16"
WalkSpeedInput.Font = Enum.Font.Gotham
WalkSpeedInput.TextScaled = true
WalkSpeedInput.ClearTextOnFocus = false
WalkSpeedInput.Parent = Frame
Instance.new("UICorner", WalkSpeedInput).CornerRadius = UDim.new(0, 6)

local SpeedLabel = Instance.new("TextLabel")
SpeedLabel.Size = UDim2.new(0.9, 0, 0, 18)
SpeedLabel.Position = UDim2.new(0.05, 0, 0, 288)
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Text = "W A S D للتحرك | Space للصعود | Shift للنزول"
SpeedLabel.TextColor3 = Color3.fromRGB(160, 160, 160)
SpeedLabel.TextScaled = true
SpeedLabel.Font = Enum.Font.Gotham
SpeedLabel.Parent = Frame

WalkSpeedBtn.MouseButton1Click:Connect(function()
    SpeedActive = not SpeedActive
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChild("Humanoid")
    if SpeedActive then
        WalkSpeedBtn.BackgroundColor3 = Color3.fromRGB(50, 180, 50)
        WalkSpeedBtn.Text = "سرعة شغاله"
        if hum then
            hum.WalkSpeed = tonumber(WalkSpeedInput.Text) or 16
        end
    else
        WalkSpeedBtn.BackgroundColor3 = Color3.fromRGB(180, 80, 0)
        WalkSpeedBtn.Text = "سرعة اللاعب"
        if hum then
            hum.WalkSpeed = 16
        end
    end
end)

FlingBtn.MouseButton1Click:Connect(function()
    flingActive = not flingActive
    if flingActive then
        FlingBtn.BackgroundColor3 = Color3.fromRGB(50, 180, 50)
        FlingBtn.Text = "الحماية شغاله"
        task.spawn(function()
            while flingActive do
                local char = LocalPlayer.Character
                if char then
                    local root = char:FindFirstChild("HumanoidRootPart")
                    if root then
                        local orig = root.CFrame
                        root.CFrame = CFrame.new(99999999999, 99999999999, 99999999999)
                        task.wait(0.02)
                        root.CFrame = orig
                    end
                end
                task.wait(0.056)
            end
        end)
    else
        FlingBtn.BackgroundColor3 = Color3.fromRGB(50, 100, 200)
        FlingBtn.Text = "حماية بانق"
    end
end)

local function stopBang()
    isActive = false
    if followConnection then followConnection:Disconnect() followConnection = nil end
    if animTrack then animTrack:Stop() animTrack = nil end
    BangBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    BangBtn.Text = "Bang"
end

local function startBang(target)
    isActive = true
    BangBtn.BackgroundColor3 = Color3.fromRGB(50, 180, 50)
    BangBtn.Text = "Stop"
    local myChar = LocalPlayer.Character
    local hum = myChar and myChar:FindFirstChild("Humanoid")
    if hum then
        local success = pcall(function()
            local bundle = game:GetObjects("rbxassetid://5938365243")
            for _, obj in pairs(bundle) do
                if obj:IsA("Animation") then
                    animTrack = hum:LoadAnimation(obj)
                    animTrack.Looped = true
                    animTrack:Play(0.1, 1, 5)
                    return
                end
            end
        end)
        if not success then
            local anim = Instance.new("Animation")
            anim.AnimationId = "rbxassetid://5938365243"
            animTrack = hum:LoadAnimation(anim)
            animTrack.Looped = true
            animTrack:Play(0.1, 1, 5)
        end
    end
    followConnection = RunService.Heartbeat:Connect(function()
        if not isActive then return end
        local myC = LocalPlayer.Character
        local targetC = target.Character
        if myC and targetC then
            local myRoot = myC:FindFirstChild("HumanoidRootPart")
            local targetRoot = targetC:FindFirstChild("HumanoidRootPart")
            if myRoot and targetRoot then
                myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 0, 1.5)
            end
        end
    end)
end

BangBtn.MouseButton1Click:Connect(function()
    if isActive then stopBang() return end
    local targetName = Input.Text
    local target = nil
    for _, p in pairs(Players:GetPlayers()) do
        if p.Name:lower():find(targetName:lower()) and p ~= LocalPlayer then
            target = p break
        end
    end
    if target and target.Character then startBang(target)
    else warn("Player not found: " .. targetName) end
end)

ResetBtn.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    if not char then return end
    local savedPos = nil
    local root = char:FindFirstChild("HumanoidRootPart")
    if root then savedPos = root.CFrame end
    local conn
    conn = LocalPlayer.CharacterAdded:Connect(function(newChar)
        conn:Disconnect()
        task.wait(1.5)
        if savedPos then
            local newRoot = newChar:FindFirstChild("HumanoidRootPart")
            if newRoot then newRoot.CFrame = savedPos end
        end
    end)
    local hum = char:FindFirstChild("Humanoid")
    if hum then hum.Health = 0 end
end)

local function stopFly()
    flyActive = false
    if flyConnection then flyConnection:Disconnect() flyConnection = nil end
    local char = LocalPlayer.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        if root then
            for _, obj in pairs(root:GetChildren()) do
                if obj.Name == "FlyBV" or obj.Name == "FlyBG" then obj:Destroy() end
            end
        end
        local hum = char:FindFirstChild("Humanoid")
        if hum then hum.PlatformStand = false end
    end
    FlyBtn.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
    FlyBtn.Text = "طيران"
end

FlyBtn.MouseButton1Click:Connect(function()
    flyActive = not flyActive
    if flyActive then
        FlyBtn.BackgroundColor3 = Color3.fromRGB(50, 180, 50)
        FlyBtn.Text = "ايقاف الطيران"
        local char = LocalPlayer.Character
        if not char then flyActive = false return end
        local root = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChild("Humanoid")
        if not root or not hum then flyActive = false return end
        hum.PlatformStand = true
        local bv = Instance.new("BodyVelocity")
        bv.Name = "FlyBV"
        bv.Velocity = Vector3.zero
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bv.Parent = root
        local bg = Instance.new("BodyGyro")
        bg.Name = "FlyBG"
        bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        bg.P = 9e4
        bg.Parent = root
        local cam = workspace.CurrentCamera
        flyConnection = RunService.Heartbeat:Connect(function()
            if not flyActive then return end
            local speed = tonumber(SpeedInput.Text) or 50
            local cf = cam.CFrame
            local vel = Vector3.zero
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then vel = vel + cf.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then vel = vel - cf.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then vel = vel - cf.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then vel = vel + cf.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then vel = vel + Vector3.new(0,1,0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then vel = vel - Vector3.new(0,1,0) end
            bv.Velocity = vel * speed
            bg.CFrame = cf
        end)
    else
        stopFly()
    end
end)

local TeleportBtn = makeBtn("تنقل", Color3.fromRGB(80, 200, 120), 293)
local SpectateBtn = makeBtn("مشاهده", Color3.fromRGB(120, 80, 220), 336)

TeleportBtn.MouseButton1Click:Connect(function()
    local targetName = Input.Text
    local target
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Name:lower():find(targetName:lower()) and #targetName >= 3 then
            target = p
            break
        end
    end
    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
        local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(2,0,0)
        end
    end
end)

local function findTargetPlayer()
    local targetName = Input.Text
    if not targetName or #targetName < 3 then
        return nil
    end

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Name:lower():find(targetName:lower(), 1, true) then
            return p
        end
    end

    return nil
end

local function stopSpectate()
    SpectateActive = false
    SpectateTarget = nil

    if SpectateConnection then
        SpectateConnection:Disconnect()
        SpectateConnection = nil
    end

    if SpectateGui then
        SpectateGui:Destroy()
        SpectateGui = nil
    end

    SpectateCamera = nil

    SpectateBtn.BackgroundColor3 = Color3.fromRGB(120, 80, 220)
    SpectateBtn.Text = "مشاهده"
end

local function startSpectate(target)
    if not target or not target.Character then
        return
    end

    SpectateActive = true
    SpectateTarget = target

    SpectateBtn.BackgroundColor3 = Color3.fromRGB(50, 180, 50)
    SpectateBtn.Text = "ايقاف المشاهده"

    SpectateGui = Instance.new("ScreenGui")
    SpectateGui.Name = "SpectateSplitGui"
    SpectateGui.ResetOnSpawn = false
    SpectateGui.IgnoreGuiInset = true
    SpectateGui.Parent = LocalPlayer.PlayerGui

    local LeftFrame = Instance.new("Frame")
    LeftFrame.Size = UDim2.new(0.5, 0, 1, 0)
    LeftFrame.Position = UDim2.new(0, 0, 0, 0)
    LeftFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    LeftFrame.BorderSizePixel = 0
    LeftFrame.Parent = SpectateGui

    local Viewport = Instance.new("ViewportFrame")
    Viewport.Size = UDim2.new(1, 0, 1, 0)
    Viewport.Position = UDim2.new(0, 0, 0, 0)
    Viewport.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
    Viewport.BorderSizePixel = 0
    Viewport.Parent = LeftFrame

    SpectateCamera = Instance.new("Camera")
    SpectateCamera.Parent = Viewport
    Viewport.CurrentCamera = SpectateCamera

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(1, 0, 0, 34)
    Label.Position = UDim2.new(0, 0, 0, 0)
    Label.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Label.BackgroundTransparency = 0.35
    Label.TextColor3 = Color3.fromRGB(255, 255, 255)
    Label.TextScaled = true
    Label.Font = Enum.Font.GothamBold
    Label.Text = "مشاهدة: " .. target.Name
    Label.Parent = LeftFrame

    local WorldModel = Instance.new("WorldModel")
    WorldModel.Parent = Viewport

    local EnvironmentFolder = Instance.new("Folder")
    EnvironmentFolder.Name = "Environment"
    EnvironmentFolder.Parent = WorldModel

    local CharacterFolder = Instance.new("Folder")
    CharacterFolder.Name = "Characters"
    CharacterFolder.Parent = WorldModel

    local clonedChar = nil
    local lastRefreshTime = 0
    local environmentRadius = 160

    local function isPlayerCharacterPart(obj)
        for _, plr in pairs(Players:GetPlayers()) do
            if plr.Character and obj:IsDescendantOf(plr.Character) then
                return true
            end
        end
        return false
    end

    local function cleanClone(clone)
        for _, obj in pairs(clone:GetDescendants()) do
            if obj:IsA("Script") or obj:IsA("LocalScript") then
                obj:Destroy()
            elseif obj:IsA("BasePart") then
                obj.Anchored = true
                obj.CanCollide = false
            end
        end
    end

    local function cloneBasePart(obj, parent)
        local success, clone = pcall(function()
            return obj:Clone()
        end)

        if success and clone then
            cleanClone(clone)
            if clone:IsA("BasePart") then
                clone.CFrame = obj.CFrame
                clone.Size = obj.Size
                clone.Transparency = obj.Transparency
                clone.Color = obj.Color
                clone.Material = obj.Material
            end
            clone.Parent = parent
        end
    end

    local function refreshEnvironment(centerPosition)
        EnvironmentFolder:ClearAllChildren()

        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart")
            and obj.Transparency < 1
            and not isPlayerCharacterPart(obj)
            and not obj:IsDescendantOf(SpectateGui)
            and (obj.Position - centerPosition).Magnitude <= environmentRadius then
                cloneBasePart(obj, EnvironmentFolder)
            end
        end
    end

    local function refreshPlayers()
        CharacterFolder:ClearAllChildren()
        clonedChar = nil

        for _, plr in pairs(Players:GetPlayers()) do
            local char = plr.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local root = char.HumanoidRootPart
                local targetRoot = target.Character and target.Character:FindFirstChild("HumanoidRootPart")

                if targetRoot and (root.Position - targetRoot.Position).Magnitude <= environmentRadius then
                    local oldArchivable = char.Archivable
                    char.Archivable = true

                    local success, clone = pcall(function()
                        return char:Clone()
                    end)

                    char.Archivable = oldArchivable

                    if success and clone then
                        cleanClone(clone)
                        clone.Parent = CharacterFolder

                        if plr == target then
                            clonedChar = clone
                        end
                    end
                end
            end
        end
    end

    local function refreshView(rootPosition)
        refreshPlayers()
        refreshEnvironment(rootPosition)
    end

    local firstRoot = target.Character:FindFirstChild("HumanoidRootPart")
    if firstRoot then
        refreshView(firstRoot.Position)
    end

    SpectateConnection = RunService.RenderStepped:Connect(function()
        if not SpectateActive or not SpectateTarget then
            stopSpectate()
            return
        end

        local char = SpectateTarget.Character
        if not char then
            stopSpectate()
            return
        end

        local root = char:FindFirstChild("HumanoidRootPart")
        local head = char:FindFirstChild("Head")

        if not root or not head then
            return
        end

        if tick() - lastRefreshTime > 0.1 then
            lastRefreshTime = tick()
            refreshView(root.Position)
        end

        local cameraPosition = root.Position - root.CFrame.LookVector * 12 + Vector3.new(0, 5, 0)
        local lookAtPosition = root.Position + Vector3.new(0, 2, 0)

        SpectateCamera.CFrame = CFrame.lookAt(cameraPosition, lookAtPosition)
        SpectateCamera.FieldOfView = 75
    end)
end

SpectateBtn.MouseButton1Click:Connect(function()
    if SpectateActive then
        stopSpectate()
        return
    end

    local target = findTargetPlayer()

    if target then
        startSpectate(target)
    else
        warn("Player not found or name is too short")
    end
end)

LocalPlayer.CharacterAdded:Connect(function()
    if SpectateActive then
        stopSpectate()
    end
end)
